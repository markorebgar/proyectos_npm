import {
  makeBorderProps,
  makeElevationProps,
  useBorder,
  useElevation
} from "./chunk-HPEOTSMG.js";
import {
  makeRoundedProps,
  useBackgroundColor,
  useRounded
} from "./chunk-JEQMMOQU.js";
import {
  makeThemeProps,
  provideTheme
} from "./chunk-KYDZXYY2.js";
import {
  useToggleScope
} from "./chunk-EJXKTA4Y.js";
import {
  makeLayoutItemProps,
  useLayoutItem
} from "./chunk-S7EDMNVQ.js";
import {
  useResizeObserver
} from "./chunk-KDWE4QBP.js";
import {
  makeTagProps
} from "./chunk-EMJ3D45R.js";
import {
  convertToUnit,
  genericComponent,
  makeComponentProps,
  propsFactory,
  useRender
} from "./chunk-T2M43BHF.js";
import {
  computed,
  createVNode,
  normalizeClass,
  normalizeStyle,
  ref,
  shallowRef,
  toRef,
  watchEffect
} from "./chunk-XX7PHJXQ.js";

// node_modules/vuetify/lib/components/VFooter/VFooter.js
import "C:/Users/Vespertino/Desktop/proyectos_npm/tienda-da2-vue-vuetify-v1/node_modules/vuetify/lib/components/VFooter/VFooter.css";
var makeVFooterProps = propsFactory({
  app: Boolean,
  color: String,
  height: {
    type: [Number, String],
    default: "auto"
  },
  ...makeBorderProps(),
  ...makeComponentProps(),
  ...makeElevationProps(),
  ...makeLayoutItemProps(),
  ...makeRoundedProps(),
  ...makeTagProps({
    tag: "footer"
  }),
  ...makeThemeProps()
}, "VFooter");
var VFooter = genericComponent()({
  name: "VFooter",
  props: makeVFooterProps(),
  setup(props, {
    slots
  }) {
    const layoutItemStyles = ref();
    const {
      themeClasses
    } = provideTheme(props);
    const {
      backgroundColorClasses,
      backgroundColorStyles
    } = useBackgroundColor(() => props.color);
    const {
      borderClasses
    } = useBorder(props);
    const {
      elevationClasses
    } = useElevation(props);
    const {
      roundedClasses
    } = useRounded(props);
    const autoHeight = shallowRef(32);
    const {
      resizeRef
    } = useResizeObserver((entries) => {
      if (!entries.length) return;
      autoHeight.value = entries[0].target.clientHeight;
    });
    const height = computed(() => props.height === "auto" ? autoHeight.value : parseInt(props.height, 10));
    useToggleScope(() => props.app, () => {
      const layout = useLayoutItem({
        id: props.name,
        order: computed(() => parseInt(props.order, 10)),
        position: toRef(() => "bottom"),
        layoutSize: height,
        elementSize: computed(() => props.height === "auto" ? void 0 : height.value),
        active: toRef(() => props.app),
        absolute: toRef(() => props.absolute)
      });
      watchEffect(() => {
        layoutItemStyles.value = layout.layoutItemStyles.value;
      });
    });
    useRender(() => createVNode(props.tag, {
      "ref": resizeRef,
      "class": normalizeClass(["v-footer", themeClasses.value, backgroundColorClasses.value, borderClasses.value, elevationClasses.value, roundedClasses.value, props.class]),
      "style": normalizeStyle([backgroundColorStyles.value, props.app ? layoutItemStyles.value : {
        height: convertToUnit(props.height)
      }, props.style])
    }, slots));
    return {};
  }
});
export {
  VFooter
};
//# sourceMappingURL=vuetify_components_VFooter.js.map
